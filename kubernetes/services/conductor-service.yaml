apiVersion: apps/v1
kind: Deployment
metadata:
  name: conductor-service
  namespace: fleet-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: conductor-service
  template:
    metadata:
      labels:
        app: conductor-service
    spec:
      containers:
        - name: conductor-service
          image: fleetmanager-conductor-service:latest
          imagePullPolicy: Never
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: global-fleet-config
          env:
            - name: APP_ENV
              value: "dev"
            - name: DATABASE_URL
              value: "postgresql://postgres:$(DB_PASSWORD)@fleet-db-postgresql.database.svc.cluster.local:5432/conductor_db?serverVersion=16.0.0"

---
apiVersion: v1
kind: Service
metadata:
  name: conductor-service
  namespace: fleet-manager
spec:
  type: ClusterIP
  selector:
    app: conductor-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
